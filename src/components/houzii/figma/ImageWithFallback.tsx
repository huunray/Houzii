// Re-export bridge — keeps relative import paths consistent after restructure.
// The canonical file lives at src/app/components/figma/ImageWithFallback.tsx (protected).
export { ImageWithFallback } from '../../../app/components/figma/ImageWithFallback';
